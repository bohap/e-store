spring:
    datasource:
        driver-class-name: org.h2.Driver
    jpa:
        hibernate:
            dialect: org.hibernate.dialect.H2Dialect
        show-sql: true

#================================================================
# App specific properties
#================================================================
book-store:
    payPal:
        mode: sandbox